name: lumora-academy CI

on:
  push:
    branches: [ main ]
    paths: [ 'lumora-academy/**' ]
  pull_request:
    branches: [ main ]
    paths: [ 'lumora-academy/**' ]
  workflow_dispatch: {}

defaults:
  run:
    working-directory: lumora-academy

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: lumora-academy/package-lock.json

      - run: npm ci
      - run: npm run lint
      - run: npm run build
      - run: npm test
